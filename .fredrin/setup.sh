#!/bin/sh
set -e

# Nothing to bootstrap yet: this repo has no dependency manifest, lockfile, or
# config template, so a fresh worktree is ready to work in as-is. Add the
# project's real setup commands here once it grows something to install.
